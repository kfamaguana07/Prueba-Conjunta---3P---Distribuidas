# API key de Google Maps (opcional). Sin ella, el mapa usa un fallback SVG.
# Obtenela en https://console.cloud.google.com → Maps JavaScript API.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=

# URL del backend (cuando esté arriba)
EXPO_PUBLIC_API_BASE_URL=http://localhost:3001
